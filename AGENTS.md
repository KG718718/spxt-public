# 公开版协作边界

本仓库是独立公开版本，不是任何公司的生产仓库。

- 用户确认：直接在本仓库操作；不在开发机另建公开版目录、克隆、工作树或保存发行包。已有内部代码仅可在被授权的范围内只读审阅。
- 所有文档、允许公开的源码、测试结果和发行物以本仓库、GitHub Actions及Releases为事实来源。
- 不复制私有仓库Git历史，不使用mirror/fork导入内部仓库；源码逐文件审查通过后才允许首次上传，不能先公开原始文件再删除敏感内容。
- 禁止导入真实业务数据、员工身份、附件、账号、凭据、内部网络地址、私有路径、部署工具、内部审计记录和含真实信息的媒体。
- 测试仅使用明确虚构的演示身份和数据；不连接任何正式系统、不发送真实业务邮件。
- 用户确认公开发行采用零业务数据交付：供应商、收款对象、客户、员工及业务记录不带入，不以改名数据或示例记录填充安装实例；测试夹具和演示种子不得进入生产安装包或首次启动自动导入。首个Admin仅由安装者创建。
- 零数据只约束新安装交付，不授权清空任何现有用户数据或内部系统；升级、重启、重装和初始化失败不得以此覆盖已有数据。
- 金额公式、权限、审批、冻结、占用、审计等保留现有已验证原则；公司专用校验和历史兼容分支的公开化必须按L2规格确认，不能以脱敏为由删除校验。
- 本任务为L2。Intent已确认；Spec中的待定项不可自行当作已批准；Plan批准前不得进入运行代码构建。
- 首次仓库引导文档可在main建立；后续实现使用codex/前缀分支。PR、合并、可用发行包发布需分别按用户授权推进。
- 不擅自新增付费云资源、永久Codespace、自托管runner、Agent或其他任务。
- 云端CI只能接收已审查公开的代码；默认只读权限、无公司secrets、无真实SMTP、无生产访问。没有证据不得声称脱敏完成、全部测试通过或支持公网部署。
