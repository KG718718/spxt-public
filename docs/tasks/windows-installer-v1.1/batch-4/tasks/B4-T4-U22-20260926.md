# B4-T4 U22 有界工程续行卡

你是执行任务，不是项目主控。TASK ID B4-T4；派单 B4-T4-U22-20260926-05；Parent Batch4；困难/L2；gpt-5.6-sol / medium。
唯一Master与return target：01a0db0e-c950-79e0-8e11-07155e0742f2。旧Master永久只读历史。
继续原thread01a0cb6a-67cc-7c03-a68b-2727af8fce6e，原worktree E:/CodexWorkspace/CodexWorktrees/f8bd/public-source，branch codex/b4-t4-upgrade-lifecycle，从local58898b2abebd465b248ca575529ca2602348df70继续，原任务baseline a3c206c48ef0bb95e054ab9a4784043a58f0ee56不改。不新建任务/worktree/Agent，不重做T1/T1A/T2/T3/T4，不fetch/rebase。
当前主控公开HEAD a035cfc05f9fcad9a52ff3f7c7f5cabbd4dd0d8b，代码与任务58898b2一致，只有主控治理文档不同。先读主控公开目录最新AGENTS/PROJECT及batch4 SPEC/PLAN/ACCEPTANCE/AUTONOMOUS-EXECUTION；本用户最新授权覆盖历史停止令和旧U22测试阶段假设。

## 用户批准（产品目标不变）
网页版明确批准方案B，恢复原T4有界续行。错误payload/program manifest hash必须在实际旧program被替换之前拒绝；故障结束后旧program、installer metadata、registration、binding、shortcuts和business instance保持或恢复到故障前精确状态。不要求发生在PrepareToInstall，不要求旧KSESSION_UPGRADE_RECOVERY_PREPARE_FAILED marker。
允许真实顺序：Gate PASS → Prepare/旧状态恢复快照 → Inno复制到staging → commit开始 → validate staged hash → 拒绝错误hash → 不替换旧program，或已开始事务时安全rollback → exact old state验证。这是测试假设澄清，不允许放宽hash、改变数据规则或接受未知身份。

## 已确认失败和预算
F2 run36214270618/job108326997302/source23ab36bff1e953e14fd5a213d1c9fb759997ee9c失败于upgrade_windows_test.go:653 U22期望prepare-failure marker缺失。实际exit4，固定marker为GATE_ACCEPTED/TRANSACTION_PREPARED/NATIVE_COPY_COMPLETE/POSTINSTALL_FAILED/TRANSACTION_ROLLED_BACK。exact owned/instance比较位于marker断言之后，未执行，不能宣称U22恢复PASS。
失败Artifact10897445184，SHA256 c6f01ed8ef890629feef8724ffbf9f04ef012e28f6168416ba94aa2792190bdb；安全报告在主控.test-work/full-review-36214270618。fresh Setup29自动PASS/3人工PENDING，D13/13及U01/U02/U15/U16/U17/U18/U20/U21八项PASS；network externalDuring=false/restored=true。
D4 run36213904038@d7ef396/Artifact10896079310是19阶段PASS（U21/U23故障非零与UNCHANGED），不能替代当前payload完整验收。
本次批准方案B新增额度：最小U22诊断最多1次、Full最多1次；前轮诊断4/4和Full2/2保留历史不重算。QA原2次未用，不能替代诊断/Full。执行无Hosted权限，主控Review后dispatch。新额度用尽仍未通过则停，无修改不得retry。

## 单一目标及验收证据
先用本地可执行反例复现真实顺序：prepare时stage不存在，之后真实stage生成，错误hash在commit的validateStaged拒绝；证明旧program替换动作未发生，并执行rollback/exact状态比较。包含正确hash正常事务、错误hash、未知/伪造诊断、状态变化等正反例。
最小修复U22测试的旧阶段假设，不能简单把expected marker换成任意失败/rollback就通过。需有可信固定事务错误原因/阶段证据（必要时事务CLI有限error-code/exit-code映射），证明受控hash拒绝在旧program替换之前。未知原因、意外swap/finalize、恢复失败均FAIL。业务instance与owned六类状态必须完整比较；禁止仅比存在性或只比data.json。
可增加窄范围固定stage/原因码到现有sequence，建议增加sequence-payload-hash专用壳及U22闭合阶段，复用既有exact beta.1 payload，不构建当前完整Portable或偷偷把最小诊断变Full。固定phase/result/state，严格keys/type/case/order和枚举；禁止原始日志、路径、request、stack、凭据、业务正文进入Artifact。不得把D4历史19项PASS用新validator倒判失败，记录新受测结构即可。
生产commit/prepare安全顺序与严格hash校验保留。如需改事务index/cli，仅为安全固定诊断和已批准语义的最小工程修复，先说明证据与范围；不得调整核心事务路线或放宽任何门禁。旧产品规则U22已由用户明确，不再要求旧prepare marker。
真实Inno/注册表/快捷方式/网络隔离仅在主控授权Hosted，不在开发机执行。无真实账号/附件/邮件，只合成数据。

## 允许/禁止范围及测试
允许tools/windows-installer/{setup.iss,build.cjs,sequence-diagnostic*.ps1,upgrade-transaction/index.cjs,upgrade-transaction/cli.cjs}、相关专项测试、tools/windows-launcher/upgrade_windows_test.go、必要workflow显式互斥诊断入口、本任务RESULT。禁止改业务源码、生产identity detection/gate/approved bundle、schema/migration/数据生命周期、UX、平台/安装路线、工具版本、版本号；不删校验、不改742语义或把SKIP当PASS。
必需相关Node事务/生命周期/CLI正反例、npm test、PS AST、Node syntax、diff-check；Go改动仅gofmt受改文件，固定Go1.27.1纯测试及compile-only，工作目录在tools/windows-launcher，不跑本机真实生命周期。保留ASCII+合成中文validator反例。
只local commit，含[sequence-diagnostic] [skip ci]；不push/Actions/main/tag/Release。RESULT更新事实、推测、变更、测试实际计数/SKIP、localcommit、D5最小运行入口和预期固定输出；完成后主动send_message_to_thread准确新MasterID并核验目标。发送失败写完整回单供主控watchdog恢复，不停在只写最终回复。
