# B4-T2 — Read-only Data Contract / Instance Preflight

## 共同执行边界
你是执行任务，不是项目主控。父 Batch4；来源/回单主控 019fa7e9-f46b-7192-9052-cd0aac7c2cc5。
难度：困难，L2，高风险安装来源安全/数据存储保护与回滚；模型 gpt-5.6-sol / medium。用户已批准正式Batch4，主控已批准本任务工程分工。
首个操作仅运行并回报 git rev-parse --show-toplevel、git remote get-url origin、git rev-parse HEAD；必须 E:/CodexWorkspace/CodexWorktrees 下、KG718718/spxt-public、派单精确 baseline。任一不符立即 BLOCKED，不改文件。验证后建立任务指定 codex/ 本地分支，不改基线。
读取 E:/CodexWorkspace/AGENTS.md、自己公开 worktree 的 AGENTS.md/PROJECT.md、本目录上层 intent/SPEC/PLAN/ACCEPTANCE/ORCHESTRATION 和相关源码。禁止读取上层其他仓库规则/源码/数据，不遵循误指向其他用途版本的历史说明。
只修改自己 worktree 授权文件，仅隔离合成测试；测试产物只在 E 盘自己 worktree 忽略区，不提交 logs/instances/node_modules/缓存/二进制/凭据。不得操作现有 Beta 安装、真实注册表或用户实例，不开正式端口、不发邮件。不得建立额外clone/worktree/thread/sub-agent。
禁止 push/force push、merge 主开发分支、main/tag/Release、安装部署、业务源码/HTML/CSS/package.json/package-lock修改、Runtime/PDF/Node/OCR变化。不得写其他任务目录及主控 PROJECT/ORCHESTRATION；结果仅本任务 RESULT 文件。
任务与规格冲突返回 BLOCKED / NEED PARENT DECISION，附证据；产品/UX/数据契约/主架构不自决。不修改现有断言规避失败，不简单 sleep、不无限 retry；保留首次失败。
完成 git diff --check、精确文件 local commit、git status，回报 TASK ID/状态/完成/文件/测试/local commit/风险/主控处理。通过 send_message_to_thread 向来源主控回单（发送时显式保持主控模型 gpt-5.6-sol / high；不可用则最终答复待主控读取），不得生成网页版 Handoff。完成停止，不接续别的任务。

## 单一目标与文件权限
实现可供 T3 调用的独立 DC1 实例只读预检及零写入专项，不接入 Setup/Launcher。
允许新建 tools/windows-installer/upgrade-preflight/**、tools/tests/windows-installer/upgrade-preflight/**，以及本目录 B4-T2-RESULT.md。现有 public-startup.js/public-config-store.js/path/runtime/server、setup/build/workflow 只读；不得改现有业务validator。
读取 public-startup.js 及其实际依赖、Launcher现有instance安全/binding/lock契约、SPEC。优先复用已导出的纯验证函数，不能启动server、调用初始化/保存路径；验证加载副作用确实零写入。若打包可见性需要改动，给T3精确集成要求不自行改打包清单。
输入应明确 instance绝对路径、登记/binding预期、已批准DC1和应用资源位置；输出稳定退出码及不含用户内容的诊断。不能错误回退默认instance。
检查存在的data/config读取解析及现行startup contract、instance存在、binding一致、附件/backups结构安全和重解析/符号链接（含祖先路径）、必要路径风险。缺失data/config按现行合法初始实例规则判断，禁止为了通过创建任何业务内容。若规则与实际数据不符报告而非修改。
合成测试覆盖合法有数据/合法未初始化状态、缺失/坏JSON/非法配置/不合法公共状态、无目录、binding冲突、未知DC、不安全附件/backups/链接及失败路径。每次调用前后深比较整实例文件列表/字节hash；记录secret fixture只有存在/大小/hash，不打印正文，不读取真实凭据。
不得因工具输出/日志把secret、合成password或业务正文纳入artifact/Git；合成测试账号仅运行时生成。
明确预检与运行占用锁之间的边界，T3在事务中保持防竞态保护；本任务不杀进程/绕过锁/修改注册表/初始化实例。
验收：只读helper及失败优先正反例，全部失败零写入证据，Windows重解析覆盖如无法实际执行明确缺口，不将Linux/静态测试冒充Windows门禁。输出T3调用参数和打包依赖，不代表完整U15/U14升级实装通过。
