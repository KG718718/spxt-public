# Acceptance
正式任务书L01—L17全部实际通过才PASS：首次启动、包内Node、无黑框、ready后浏览器、二次双击/健康复用、8080冲突自动换端口、不杀外部程序、Node异常退出、缺Runtime、不可写instance、中文/空格路径、日志无秘密、loopback、只停自己、Runtimehash不变。
额外：Launcher异常退出Job回收、并发首次启动、过期锁、伪造/错误PID不复用、manifest篡改拒绝、端口全占、浏览器失败服务保留。
Windows10开发机必须实际EXE；Hosted Server不替代Win10。全自动测试不能代替系统默认浏览器/普通用户控制窗口验收。未验证标BLOCKED，不能降低门禁。
