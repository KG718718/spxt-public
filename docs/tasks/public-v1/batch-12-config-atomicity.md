# 第12批公开审查｜配置与审计一次保存

日期：2026-09-11。仅公开候选；用户明确批准 PUBLIC-CONFIG-ATOMIC-001 修复。

## 失败优先

- 原配置路由合成反例已复现：虚构税率0.02变0.03，配置持久化成功后审计失败，返回400但参数已变。来源完整 server 仍未上传；不是实际业务事故。
- 本批63项内存专项先在缺少实现时失败；已有启动校验对“版本1但审计为空”错误放行，新增断言先失败。
- 实现后63项内存专项、原启动80项通过。云端 HTTP/真实文件系统和全量回归待本次提交运行，不提前记通过。

## 实现与数据契约

新增 public-config-store.js / public-config-http.js，public-startup.js 复用严格参数/审计校验。
允许业务字段继续是 taxRate、serviceFeeRates、bonusRules、invoiceBuyerName。单个参数组未提供则保持原状态；明确提交时完整校验，不含默认6%或公司档位。未知旧根字段保存原值但不进入 API 或审计，新请求不可写入未知字段或公式。

configVersion 与完整 configAuditRecords 保存在同一个 config.json 内；旧配置缺两字段时只读按版本0，首次成功保存才生成审计，不初始化迁移。审计按版本递增，记录id/time/user/action/changedFields/before/after；不作500条截断、不写入第二个 data.json。原 /api/logs 将其投影为 time/user/action/detail 并保留旧日志，只有 Admin 可读。财务业务数据与历史快照不被此组件写入。

同目录 wx 临时文件、fsync、字节核对、原子替换；内存只在替换成功后更新，没有第二次审计回调可将成功变成失败。保存前故障保持旧配置与内存；只清理自己成功建立的临时文件，名称碰撞不删除其他文件。保存前发现文件外部变更拒绝覆盖。

HTTP保持 /api/config GET/PUT 与 /api/logs GET；Admin 开始与收完请求两次实时权限核验，同源浏览器写入、JSON、64KiB请求上限及超时；员工配置只读允许字段。expectedVersion 必须合法，旧版本409。网络断开不等于服务端一定没保存，应刷新核对当前版本，不能自动重复或回滚。

## 验收与已知边界

内存专项包含显式0、非法/缺参数、权限、版本、未知字段、审计/响应副本不可变、501条保留、各阶段失败、外部变动、损坏/链接拒绝和重载。
云端合成 HTTP 专项覆盖权限、原方法契约、请求中撤权、并发单成功、真实磁盘故障、原业务文件逐字节保留、独立子进程重启和损坏审计拒绝；认证适配器是明确虚构身份，不是完整登录验收。

仍使用单服务进程的文件存储；外部文件变动检查不是多进程共享事务保证，fsync/rename也不宣称所有硬件断电均可恢复。审计低频但文件会增长；未来归档必须可追溯且另行设计，不可静默截断。完整 server 与 Admin 页面尚未接入，后续必须替换旧配置双写路径，使用本存储对象作为唯一参数权威并从旧操作日志入口读取新审计；组件通过不代表封装完成。

## 范围

全新公开代码、合成测试和增量记录只通过 GitHub API/托管CI保存。无开发机文件/公开副本、无内部代码改动、无真实数据/附件/邮件/正式主机操作；无新依赖、PR、main合并、Release或部署。

## 首轮云端实测

提交4724d9141a9a2f4ee9b87045a991b477bcde56bc，托管Windows CI34569921053成功：17份/602项（新增内存63、真实HTTP34），失败0、依赖审计0；独立进程重启与磁盘写入/fsync/rename失败、请求中撤权、并发与旧请求均通过。完整业务认证和Admin页面仍未接入。

收尾再增加2项内存处理器反例：原日志服务不可用不影响已原子提交的结果；提交成功后响应连接断开，不回滚、不再发送虚假的失败响应，旧版本重试409。新增后本专项65项通过，待最终云端全量复跑。

## 2026-09-11 配置原子保存组件最终验收

- 代码提交：af02673769410013887bcce08356a2513dde0ba8（实现提交4724d9141a9a2f4ee9b87045a991b477bcde56bc）。
- 托管 Windows [CI 34570119060](https://github.com/KG718718/spxt-public/actions/runs/34570119060) 成功；17份测试共604项，失败0，依赖审计0。新增专项65项内存+34项真实HTTP/文件系统，共99项；不把同项内存复跑再次计入总数。
- 验证参数/审计同文件提交、写入/fsync/rename失败原字节及内存不变、同版本并发仅一个200另一个409、旧请求拒绝、不调用第二份日志保存、独立进程重启、损坏审计停止启动、请求接收中撤权、提交前断开零写入、提交后响应断开不假报失败或回滚。历史业务data文件及快照逐字节保持。
- 原操作日志HTTP投影与Admin/员工/审批岗范围已测，完整系统会话/登录和Admin页面接入尚未完成；本次没有新增配置页面或其真实浏览器证据。既有初始化/登录页面组件15项随全量复跑通过，不能用于冒充配置页验收。
- 云端登录组件截图artifact10187456538，许可材料artifact10187456015；不是安装包。五个运行/测试文件远端内容与RAM逐字节相同并核对SHA256，当前候选67个文件中无私有RAM源、运行数据、配置、附件或备份。
- main仍7eb1ebe17450301aa0f290d55a5a0d3dd7ff2a6e。只提交公开候选；未建本地公开副本、未改内部tracked代码、未访问19/真实数据/SMTP、未创建PR/合并/Release/部署。

### 下一条明确动作（无需重复确认本次修复）

继续完整server逐段公开审查与接入：唯一参数权威使用createConfigStore；既有/api/config和/api/logs委托createConfigHandler，删除原config保存后logAction双写路径，不在回调再复制保存配置。同步Admin现有配置表单的expectedVersion、百分比/明确0/未配置、购买方和奖金可见字段，再做完整角色、配置保存、日志查看、财务快照与跨模块HTTP/浏览器验收。未完成前不宣称封装完成或提供可用安装包。
