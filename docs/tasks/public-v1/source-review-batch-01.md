# 首批基础源码公开审查｜Batch 01

日期：2026-09-10。状态：上传前审查与测试通过，远端回读结果另附。仅本批允许文件获上传，不代表完整系统脱敏或可运行。

## 允许清单

| 文件 | 内容 | 数据与副作用 |
|---|---|---|
| client-master.js | 甲方名称、别名校验、稳定ID引用、快照与历史绑定纯函数 | 只处理调用方传入值，不包含预置甲方、不访问文件或网络 |
| business-view-contract.js | 欠款权威状态读取 | 无预置欠款、无写入 |
| tools/tests/public-foundation.test.js | 24项新编单元测试 | 明确虚构身份与记录；不创建磁盘业务数据，不启动服务；不得打入生产包或自动导入 |

两份基础源码原样保留，未改业务判断；新测试不复制原内部测试中的页面依赖或真实记录。检查关注名称规范化、别名冲突、停用约束、显示字段最小化、快照、历史绑定和状态优先级，不把它们冒充完整API权限或业务流程验收。

## 上传前检查

- 三个文件已完整阅读；定向标记检查未发现已知公司/员工标记、内部路径/地址、私钥/令牌或凭据字面量。
- 语法解析通过；在仅允许本批模块和Node内置assert的内存加载器中执行测试，24/24通过。
- 测试无正式数据、文件写入、业务HTTP服务、SMTP或其他网络访问；不创建本机公开源码副本。
- 模块没有预置记录，仅证明本批不携带数据；全新安装零数据及首个Admin初始化尚未实现，不能标记通过。
- 模式检查不是全仓库秘密扫描或安全证明；未上传文件、原私有Git历史、媒体、构建日志及未来安装包不在本批结果范围。

## 文件SHA256

- `client-master.js`：`59351ca617997807de913299f1849dd062fede64262fc5c80576219b10f5ffe6`
- `business-view-contract.js`：`b7739470b0116f6a7e45ac47afd7ee9f13819f0b7b9b7c0f85003f2ae8ee4c0a`
- `tools/tests/public-foundation.test.js`：`a052eca77e60d301f429e3f571444d6d9e188916ec4abc6f68728a4990e21a12`

## 执行边界与未完成

仅在独立公开仓库的候选分支保存。未合并main、创建PR、运行云端CI、生成Release或安装包；未改变原内部系统、真实数据和运行环境。公司专属入口改造、首次Admin、购买方/替票授权、奖金默认值、平台网络与许可问题仍以Spec/Plan为准。

本测试可在文件齐全的受控环境通过 `node tools/tests/public-foundation.test.js` 执行；该命令不会启动完整系统。测试源码不进入生产安装包。许可证尚待明确，本批上传不自行声明开源许可。
