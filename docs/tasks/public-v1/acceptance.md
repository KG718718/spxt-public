# Acceptance｜K⁺-SESSION 公开版 V1

## 2026-09-10 最新批准：完整公开化构建与云端验收

用户已确认上一轮提出的安全边界：首版Windows；损坏/不可读/关键结构非法的数据及配置拒绝启动、保留原件；只有真正首次安装可在主机本机创建首Admin，成功后关闭入口；公开版移除服务器命令执行类运维接口。允许仅在本公开仓库使用GitHub托管Windows完成自动测试、角色浏览器及候选包构建。

主控批准依照已经确认的品牌、零数据、购买方配置、员工替票授权、专属机制移除、服务费/奖金全部外显及本次安全边界，连续执行以下技术Plan，不再按普通子批次反复请求上传许可：
1. 安全加载/初始化与失败优先测试；本机同源限制、重复与并发拒绝、无默认凭据、已有数据/附件不覆写。首次缺data但残留已有业务资产时不自动当新安装。
2. 完整来源运行文件逐文件公开审查，白名单迁入候选；只在内存处理，不将公开版文件落地开发机。沿用现有Node架构与业务门禁，不复制真实配置、记录、私有工具、历史或媒体。
3. 接入服务端/Admin参数、购买方与员工替票权限，去掉已确认的专属名称及公司默认兜底。补全5个既有页面与必要设置入口，保持既有角色、公式、历史、附件和备份恢复规则。
4. 云端Windows runner执行纯合成单元、HTTP、权限、数据保存/恢复及跨模块回归；失败先修再重跑。工作流固定依赖和动作版本、contents只读、无公司secret/真实SMTP/生产连接，不启用自托管runner或额外付费资源。
5. 云端真实浏览器验证初始化、Admin/审批岗/员工5页关联流程与默认/焦点/悬停/禁用/弹窗/表格对比度；截图和结果只留云端。
6. 云端按允许清单构建候选ZIP，运行文件/依赖清单和SHA256随包；生产包排除测试、种子、业务文件、日志和已有配置。解包再验证零数据、初始化、重启和升级保留合成数据；OCR可选未安装必须明确提示。
7. 修复与现有确认一致的缺陷后重复相关门禁，保存云端证据。遇到需改变业务规则、重大安全方案、授权不足或真实双机验收不可执行时再与用户沟通。

本节替代下文历史“尚不运行CI/仅核心子步骤获批”的阶段限制；历史证据保留，不把旧未执行改成已通过。仍不授权PR、合并main、Release、正式部署或改内部实例；许可证/真实局域网双机设备待单独确认。税费范围按既有待决项处理，不能因本次批准猜测新税率。


当前状态：上一批托管 Windows CI 通过 237 项、6 份测试；本批欠款/邮件/备份模块和依赖已提交候选，托管 Windows CI 全部 279 项、9 份测试通过，npm audit 报告 0 漏洞。员工奖金导出范围反例已确认，导出源文件未上传，等待用户确认可见范围。完整服务、页面、HTTP/浏览器和封装仍未完成；不得把模块或文件系统检查作为全系统验收。

- [x] 独立公开仓库建立，未改变原仓库可见性。
- [x] 仓库引导阶段仅新编计划文档；后续基础源码按下列允许清单上传，仍无公司数据或私有提交历史。
- [x] 未在开发机克隆、创建公开工作树或保存公开发行物。
- [x] 2026-09-10已登记用户确认：K⁺-SESSION品牌、首次安装自建管理员、OCR可选（仅需求确认，不是实现通过）。
- [x] 已登记用户确认：去掉原公司名称和Logo；购买方改由Admin配置，未配置或不匹配拒绝提交（仅规则确认）。
- [x] 用户有条件确认员工替票由Admin配置：只替换固定名单，其他现有业务规则不变；尚未实现或验证。
- [x] 用户确认零业务数据交付，包含供应商/收款对象；不附预置账号、改名旧数据或演示种子（仅规则确认）。
- [x] 用户授权：公开审查/脱敏通过后直接上传合格内容，不重复询问同一上传许可；未审查原始内容不在授权内，本项不代表源码已脱敏。
- [x] 用户授权继续上传；主控批准首批原样源码上传子计划，不涉及未决业务改造。
- [x] 首批client-master.js、business-view-contract.js完整阅读与定向敏感标记检查，24项内存合成单元测试和语法通过。
- [x] 首批上传后回读：11项文件清单一致，两个模块及测试SHA256一致；远端内容在内存复跑24/24，非云端CI或完整系统验收。
- [x] 用户确认服务费率、奖金档位及业务系数不预置公司数值、由Admin外显维护；缺配置不得按0或旧默认计算（产品确认）。
- [x] 服务费核心子计划先批准；49项失败优先测试、适配及上传完成，远端回读5个源码/测试哈希一致，共73项内存单元测试通过，候选文件允许清单一致。
- [x] 奖金核心子计划已批准：配置校验、无默认/历史回退、动态人工阈值、过期摘要409、完整快照；73项专项与200组差分通过，上传后9个源码/测试哈希和19项文件清单一致。
- [x] Admin字段与API调用接入矩阵已固定；此项仅文档，不是页面实现或验收。
- [ ] Admin服务费页面与server接入完成，无页面或后台兜底数值；配置保存/角色/审计/未配置错误与真实页面验收。
- [ ] 奖金档位及相关业务阈值/系数全部外显，无公司历史算例回退；区间校验、完整性、权限、规则快照及锁定/月结回归通过。
- [ ] 配置变更不会重算或覆盖已保存的历史费率、金额和奖金规则快照；缺历史快照不静默套新配置。
- [x] 顶部完整公开化技术 Plan 已批准；业务未决项、许可证、真实局域网和正式发行仍须单独确认。
- [ ] 代码及素材权利、许可证明确。
- [ ] 逐文件脱敏与公共配置实现。
- [ ] 页面、图标/图片、邮件、导出/打印、文档和安装包无原公司名称或Logo残留；K⁺-SESSION显示正确。
- [ ] 最终安装包允许清单验收：无主数据/业务记录、旧附件/缓存/导出/日志/备份、邮件配置或测试种子，不只检查一个数据文件。
- [ ] 干净新安装首次启动不自动导入记录；首Admin创建前账号为0，创建后仅有使用者创建的Admin和必要安装审计，业务与主数据仍为0。
- [ ] 一次性管理员初始化完整验收。
- [ ] Windows干净环境安装与升级不覆盖数据。
- [ ] 购买方及替票授权等配置边界验证通过，未因去公司品牌而删除服务端校验。
- [ ] 已确认员工授权方案完成实现；账号关联不自动授予替票，越权/撤销/停用/旧草稿递交验证，历史分类及余票归属不变。
- [ ] 虚构账号与等价授权输入的行为对照通过；项目/角色权限、发票占用与余额、历史记录及金额公式无未批准差异。
- [x] 用户选择取消原公司专属收款机制，不指定替代对象或增加替代配置（仅决定确认，尚未实现）。
- [ ] 原公司专属身份、授权、欠款准入及银行豁免已移除；前后端与空态一致；未误放开所有公司对象、未误删通用收款类型/员工收款能力、未改来源系统；付款、发票、历史分类与银行重复校验回归通过。
- [ ] OCR已安装/未安装两种路径验收，依赖与联网说明真实。
- [ ] 模块和跨模块回归通过。
- [ ] 各角色真实页面、视觉、错误态与刷新验收。
- [ ] 云端CI、秘密扫描、依赖及发行包清单通过。
- [ ] 打包与哈希存云端，未下载本地。
- [ ] 独立主机与第二客户端完成局域网实测，包含加密/权限、断网、重复/并发提交和重启。
- [ ] 最终发行包在干净环境复验，非仅开发版本或CI验证。
- [ ] 用户正式发行确认。

网络测试当前未执行。测试设备、安装授权与最终网络方案尚待确认；云端CI不能替代真实局域网验收。

CI日志、截图、文档及下载包也属于公开审查范围；不得把没有data.json当成完整脱敏。

## 2026-09-10 启动预检（未实施修复）

4项虚构内存加载检查完成：缺文件空状态、坏数据JSON空状态并记错、坏配置JSON空状态并记错、合法虚构数据保留。仅执行来源加载代码段，不代表完整服务测试；无真实数据读取、无保存或本地文件写入。

新增必过门禁：损坏/不可读/非法结构的数据及配置不能进入首次初始化，必须拒绝启动并保留原件；首次本机Admin、重复/远端拒绝、升级重启保留数据、公开包不含远程命令运维入口均需实现与反例测试。建议方案待用户确认，不能标为通过。

云端HTTP、角色浏览器、安装封装尚未执行，现有146项核心单测不能替代。未修改现有模块或重跑无变化测试；候选尚不可安装。完整记录见Plan末尾。

## 2026-09-10 本轮云端基础验收与暂停点

- [x] 安全初始化核心与真实文件系统单独验收，237 项先行批次通过；无默认凭据、损坏/不可读/符号链接拒绝、重启和双进程初始化竞争均有合成证据。
- [x] 本轮 279 项、9 份测试通过，新增欠款 15、邮件 18、真实备份 9；依赖审计 0 漏洞。证据：[托管 Windows CI](https://github.com/KG718718/spxt-public/actions/runs/34462172034)。
- [x] 公开候选允许清单不含 export-service.js；员工导出范围问题仅代码与虚构反例，未访问正式系统。
- [ ] 用户确认员工奖金导出范围后，补失败优先测试、最小修复、元数据和聚合金额不泄露验证，再上传导出模块。
- [ ] 完整服务端、页面、HTTP/浏览器、安装与升级/完整恢复、实际双机 LAN 门禁仍未完成；目前无安装包。

候选源码提交：207a2576fdcb19d5f3bb5dd975e5b1499513d14f。不修改公司版、正式主机或本地业务资产；不创建本地公开副本、PR、合并或 Release。历史阶段记录保留，以本页顶部与本节最新结果为准。
