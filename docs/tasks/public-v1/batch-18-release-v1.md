# 第18批｜K⁺-SESSION 1.0.0 发布

日期：2026-09-12。沿用已批准 L2，本批是发布阶段，不新增产品规则或实例迁移。

## 授权与范围

用户确认仅本仓库创建 PR、合并 main，发布 v1.0.0 正式 Release、在线安装 ZIP、SHA256 与更新说明。不在开发机保存源码或发行物，不更新任何已安装实例，不连接真实业务系统，不发真实邮件，不重写历史或删除分支。

## 已核对事实

- 完整候选 f5fc23f6a7c015d2ef55b882cc3ffc82b139a6a1；其受测代码 f5d7663f8d699bbf32d719b1a28bf916188c3ae4，之后仅文档。
- CI 34609338291：742 项核心、23 项安装、6 项可选 OCR 通过，依赖审计 0。
- main 7eb1ebe17450301aa0f290d55a5a0d3dd7ff2a6e 仅含 7 份早期治理文档。其 9 个独有提交只改 PROJECT、README、Intent、Spec、Plan、Acceptance；没有独有程序、业务数据或安装器。
- 当前完整候选保留对应批准与历史记录，且已完成产品化文案、规则实现和验收更新。解决文档冲突采用已审核的候选版本，不将早期未实现状态或过期介绍带回首页；通过双父提交保留两边历史。

## 执行顺序

1. 更新 README、CHANGELOG、版本规范与发布记录；不改业务源码、安装器、依赖、网络或版本数字。
2. PR #1 从 codex/public-v1 合并到 main。只解决上述治理文档的历史分叉；合并前核对分支 SHA，保留两边父提交，不强推。
3. 在最终候选执行托管 Windows 完整 CI，包含核心、实际安装及可选 OCR。失败不发行；保留真实结果和限制。
4. 合并时固定已审查的 head SHA；校验 main 合并文件树与受测树完全一致，不一致则重新测试。
5. 将 v1.0.0 绑定该 main 提交；先建草稿 Release，在内存提取云端已测试的内层安装 ZIP。复核版本、允许清单、逐文件哈希、无实例与测试数据、MIT/第三方声明。
6. 上传安装包、SHA256、包清单和说明；回读 Release 资产验证字节和 SHA256 后才正式发布。发行包不混入云端合成截图或日志。
7. 在 PR 与 Release 保存最终提交、CI、文件树、资产与校验回单。发行不是部署，不触碰任何使用者实例。

## 安装及升级边界

Windows x64 在线安装；首次需访问官方依赖来源。全新安装零业务数据，本机创建首个 Admin，业务参数由 Admin 配置；OCR 可选。默认 127.0.0.1，可显式改端口；LAN 需独立配置与验证，真实双机尚未验收，不承诺直接安全公网部署。

无代码签名或离线二进制整包。升级前停机完整备份；当前只支持安装器明确识别的相同实例结构，不是任意版本迁移。后续版本按 1.0.1 / 1.1.0 / 2.0.0 递增，不覆盖已发布标签或安装包。

最终事实以 [PR #1](https://github.com/KG718718/spxt-public/pull/1)、[v1.0.0](https://github.com/KG718718/spxt-public/releases/tag/v1.0.0) 的实际完成记录为准。本计划本身不冒充已合并或已发布结果。
