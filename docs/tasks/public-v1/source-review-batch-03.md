# 奖金配置、预览与锁定公开审查｜Batch 03

日期：2026-09-10。状态：上传前检查、上传及远端回读测试通过。当前任务串行，无Agent/新任务/工作树；公开内容只保存于独立GitHub仓库。

## 允许清单及实现

| 文件 | 实际内容 |
|---|---|
| bonus-config.js | 新编配置校验；两类档位、边界与人工阈值、规则摘要；无预置业务值 |
| bonus-preview.js | 删除预置业务档位与历史算例回退；显式配置试算、动态人工阈值、无配置待确认 |
| execution-bonus.js | 接入规则摘要409门禁和完整快照；去掉已确认移除的公司特名排除；其余确认规则保留 |
| tools/tests/public-bonus-config.test.js | 73项新编合成单元测试；参数、账号与记录均虚构，不是安装种子 |

空表是未配置结构，不是默认0元制度。金额两位、系数及KPI边界六位精度；拒绝未知字段、非有限/不合法数值、倒置或重叠档位。单表最多100档为资源保护，不是公司奖金制度。无界限用null，明确0有效。未覆盖某项目的档位仍待确认，不补0、不扩大人工确认范围。

正常公式和原Admin最终金额调整规则保留；低KPI2人工确认依据Admin明确配置的阈值，仍需备注。公式不是可编辑代码。预览返回规则内容摘要，确认必须匹配当前服务端规则；摘要不是权限令牌，路由仍必须独立校验实时Admin角色。

新奖金快照保存完整参数、阈值、摘要和既有公式说明。调用不回写已锁定记录或输入数据。保存/HTTP/页面接入尚未实现，不能凭纯函数结果称实际月结、数据库原子性或角色权限已通过。

## 失败优先

在只读来源模块上使用5个固定反例，均按新契约预期失败：默认配置仍可试算、存在硬编码历史算例、人工阈值未跟随配置、未拒绝旧规则摘要、快照未包含可变阈值。这是公开化契约差异，不宣称既有运行实例发生了5个正式数据事故。原值和原失败输出未上传。

## 上传前测试与审查

- 73/73合成单元测试通过：配置缺失/非法、0、精度、范围/边界/重叠、摘要、正常/人工试算、付款状态与日期、执行人员/0元原因、重复锁定、过期配置、快照不回写。
- 另以200组合成数据对照只读来源的财务计算、有效付款选择及显式同参数正常奖金计算；200/200结果一致。该差分涉及不可公开的来源文件，仅公开非敏感结论，不上传原文件/预置业务参数或把它伪装成使用者可独立运行的公共测试。
- 四文件完整审阅、语法检查及定向公司/身份/内部路径地址/凭据标记检查通过。预置组织表、具体历史项目和身份均未进入公开源码。
- 测试只使用内存模块、Node内置assert/crypto；不写本机公开文件、不启动服务、不读取业务数据、不发邮件。模式检查不证明整个未上传系统/发行包脱敏完成。

## SHA256

- `bonus-config.js`：`ec05aeccb99dec94c0f20fbd5a8cbdf5f4bfcb19e7221c39bf3ea96686217729`
- `bonus-preview.js`：`1208f67bc963bcd28b365681f2baf117912c2972a2f86a48137593ebc918446f`
- `execution-bonus.js`：`7d65e271486c290d87af661d5678e50fd8c4a5f358a88acbfd4b6a685c5eb73e`
- `tools/tests/public-bonus-config.test.js`：`ada866c5fe093b4e8cf6d71659134042a632f5d94575eaba8dde0a621475922d`

## 管理页和服务端下一接入点

Spec已列页面字段矩阵：服务费单项下拉/百分比、利润区间/奖金金额、KPI区间/系数、人工阈值、规则变化刷新、历史快照只读。

原配置API尚未校验奖金，预览与锁定调用也未传动态规则；Admin页面仍有原阈值文案。因此本批不上传未审查的完整server/admin文件，不另建空壳管理页冒充接入。后续必须在对应文件完成公开审查后接入config.bonusRules和rulesDigest，再验收配置保存、权限、审计/保存失败、旧页面提示、视觉和奖金/月结/邮件/导出联动。

未完成：实际Admin配置页面、HTTP及持久化、真实浏览器、完整月结关联、云端CI、安装/升级/局域网、发行包与许可证。未创建PR、合并或Release；内部源代码、数据、附件和正式主机均未改。

## 上传后回读

- 源码提交：`77d5ff4c03e388c9bca19a289ece0706106a135d`，候选分支codex/public-v1。
- 19项文件与允许清单逐项一致，无额外文件；9个源码/测试文件SHA256与各自审查版本一致，旧批次源码未变。
- 从GitHub回读后执行基础24项、服务费49项、奖金73项，共146项全部通过。纯内存执行，无本地公开文件；不是GitHub Actions CI。
- 没有HTTP、真实浏览器或安装包验收结论。管理界面外显和服务端路由未完成，不能据此交付可用系统。
