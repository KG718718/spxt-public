# Plan｜K⁺-SESSION 云端公开版 V1（原样上传与服务费核心子步骤已批准，完整改造待批准）

## 2026-09-11 继续：MIT 许可证落地（当前决定）

用户已确认 MIT 并要求直接执行，本次明确恢复公开版封装。使用标准 MIT 正文，项目级中性署名采用 Copyright (c) 2026 K-SESSION contributors，不披露公司或真实个人名；这是项目贡献者集合署名，不推断任何具体公司权属。第三方作品仍保留原作者和原许可，不用项目 MIT 覆盖第三方条件。

本次允许根 LICENSE、package.json/锁文件根许可字段、README/第三方说明、许可核验和合成测试，全部只入公开候选。主控沿用已批准 L2 范围串行推进，不创建任务或本地副本。完整原生组件/Node/OCR/素材及最终包逐项核对仍需完成；不合并 main、不创建 PR/Release、不部署。


## 2026-09-10 最新批准：完整公开化构建与云端验收

用户已确认上一轮提出的安全边界：首版Windows；损坏/不可读/关键结构非法的数据及配置拒绝启动、保留原件；只有真正首次安装可在主机本机创建首Admin，成功后关闭入口；公开版移除服务器命令执行类运维接口。允许仅在本公开仓库使用GitHub托管Windows完成自动测试、角色浏览器及候选包构建。

主控批准依照已经确认的品牌、零数据、购买方配置、员工替票授权、专属机制移除、服务费/奖金全部外显及本次安全边界，连续执行以下技术Plan，不再按普通子批次反复请求上传许可：
1. 安全加载/初始化与失败优先测试；本机同源限制、重复与并发拒绝、无默认凭据、已有数据/附件不覆写。首次缺data但残留已有业务资产时不自动当新安装。
2. 完整来源运行文件逐文件公开审查，白名单迁入候选；只在内存处理，不将公开版文件落地开发机。沿用现有Node架构与业务门禁，不复制真实配置、记录、私有工具、历史或媒体。
3. 接入服务端/Admin参数、购买方与员工替票权限，去掉已确认的专属名称及公司默认兜底。补全5个既有页面与必要设置入口，保持既有角色、公式、历史、附件和备份恢复规则。
4. 云端Windows runner执行纯合成单元、HTTP、权限、数据保存/恢复及跨模块回归；失败先修再重跑。工作流固定依赖和动作版本、contents只读、无公司secret/真实SMTP/生产连接，不启用自托管runner或额外付费资源。
5. 云端真实浏览器验证初始化、Admin/审批岗/员工5页关联流程与默认/焦点/悬停/禁用/弹窗/表格对比度；截图和结果只留云端。
6. 云端按允许清单构建候选ZIP，运行文件/依赖清单和SHA256随包；生产包排除测试、种子、业务文件、日志和已有配置。解包再验证零数据、初始化、重启和升级保留合成数据；OCR可选未安装必须明确提示。
7. 修复与现有确认一致的缺陷后重复相关门禁，保存云端证据。遇到需改变业务规则、重大安全方案、授权不足或真实双机验收不可执行时再与用户沟通。

本节替代下文历史“尚不运行CI/仅核心子步骤获批”的阶段限制；历史证据保留，不把旧未执行改成已通过。仍不授权PR、合并main、Release、正式部署或改内部实例；许可证/真实局域网双机设备待单独确认。税费范围按既有待决项处理，不能因本次批准猜测新税率。


任务等级L2：涉及隐私、安全、首次管理员权限、跨模块配置和对外分发。当前任务串行处理，不新建Agent/工作树/其他任务。

## 2026-09-10 首批原样上传执行子计划

用户已授权“脱敏通过后直接上传”，本轮再次要求“继续上传”。主控批准仅执行以下原样上传子步骤，不把尚未决定的产品改造当成已批准：

- 目标分支：`codex/public-v1`，只在本公开仓库创建，不建立本地工作树，不引入私有Git历史。
- 允许：逐行审查后原样上传`client-master.js`与`business-view-contract.js`；新增仅虚构数据的`tools/tests/public-foundation.test.js`；同步本批审查记录和状态文档。
- 两个模块均为调用时处理传入记录的基础函数，无数据库种子、登录身份、公司参数、自动初始化、文件/网络访问或第三方依赖；不得因上传而修改其既有业务判断。
- 上传前：完整阅读源文件、定向敏感标记检查、语法与24项纯内存单元测试；上传后：核对允许清单/哈希并按远端内容复验。
- 禁止：服务端、页面、未决公司参数、实际数据/附件/配置、私有工具/历史；不运行CI、创建PR、合并或发行包，不改任何内部系统。
- 该步骤不等于整份Plan批准或系统脱敏完成；尚含单位专用内容、默认费率或未决规则的模块继续暂缓，不通过删除业务校验凑齐源码。

## 阶段一：仓库与规格

已建立独立公开仓库，并重新编写治理文档。2026-09-10已同步用户确认的品牌K⁺-SESSION、首次安装创建管理员及OCR可选要求；这三项不再重复询问。随后已确认去掉原公司名称和Logo、购买方由Admin配置且保留校验。员工替票授权方案也已获有条件确认，仅更换资格配置来源，其余原有规则保持。用户随后选择取消原公司专属收款机制，不指定替代对象或增加配置；该项不再待确认。服务费与奖金不预置公司数值、由Admin外显配置现已确认；继续确认spec中的初始化安全边界、Windows首发范围及局域网方案；许可证未选定前不宣称开源授权。零业务数据交付已确认：不带任何供应商或收款对象等预置主数据，不向使用者实例导入合成数据。完整改造Plan仍未批准；生产修改只允许本文件明确批准的分步范围，不因此开始其余未决改造。

## 阶段二：逐文件脱敏

- 只读审阅已验收来源，不上传其目录、历史或构建产物。
- 首批审查范围：五个HTML页面、主题CSS、server.js、bonus-preview.js、execution-bonus.js、employee-settlement.js、debt-repayment.js、client-master.js、service-fee-config.js、business-view-contract.js、export-service.js、backup-service.js、mail-reminder.js、smtp-secret-store.js、package及锁文件、必要OCR脚本。
- 原公司名称与Logo须逐项检查页面、图标/图片、邮件、导出/打印、文档和安装包，不只处理首页；公开品牌使用K⁺-SESSION，不新建未经确认的图形Logo。
- 每份文件先检查单位信息、员工身份、地址/路径、真实金额算例、凭据和隐藏依赖；审查后的公共内容直接提交至codex/public-v1分支。
- 文件内容可以在当前会话内只读审阅和内存处理，但不写入本机公开版目录；未审查文件不得进入公开库。
- 测试只迁入必要且经过审查的合成测试；合成数据仅在隔离测试环境使用，不搬内部日志/媒体，不作为初始业务数据导入生产安装。
- 生产包从干净源码及明确允许清单构建，不直接打包试跑目录；排除业务数据、主数据种子、测试账号、附件、缓存、导出、日志、备份和邮件配置/记录。

## 阶段三：适配与失败优先测试

- 首Admin初始化：首次成功、重复拒绝、非本机拒绝、无默认密码、损坏数据不覆盖。
- 零业务数据验收：解包后无预置身份、供应商/收款对象或业务文件；首次启动不注入种子。创建首Admin前账号为0，完成后只产生用户创建的Admin及必要初始化审计，主数据/业务记录仍为0；再以隔离虚构数据验证用户自行建账号/主数据和关联的流程。
- 将升级/重启的数据保留与新安装空库分开测试；带有虚构旧业务的升级样本必须原样保留，不调用清空脚本，也不把验收后的数据目录打入最终包。
- 购买方：Admin配置自己的单位全称；未配置/名称不匹配拒绝、匹配时仍执行其他既有发票校验、员工越权配置拒绝、直接payload不能绕过。不得把购买方匹配等同于自动审核通过。
- 员工替票授权方案已获有条件确认；整份Plan批准进入实现后，验证新账号默认未授权、Admin从有效账号列表勾选、员工/审批岗越权维护拒绝、仅关联收款对象不授予资格、授权员工处理正常替票、正式供应商销售方不匹配仍拒绝。
- 授权撤销/账号停用后旧草稿递交重新校验，直接payload和身份近似名称不能绕过；已确认发票、原金额、收款类型和池归属不被授权更改。不得将操作权限名单复用于动态重分类历史。
- 公司专属收款机制已确认移除：不新增替代映射。先固定原特例在银行信息和欠款前后端筛选中的反例，再移除专属身份/授权、欠款准入及银行字段豁免；验证正式供应商正常可选，员工和其他公司收款对象不因删除条件而获得新的欠款资格，空态/文案与服务端一致。保留通用收款类型及其他既有业务规则；不修改来源公司系统。该项实现仍须整份Plan批准。
- 员工替票兼容性门禁：用虚构账号及等价授权输入对照原业务判断，项目可见性、角色权限、供应商校验、占用/余额、历史快照和金额结果不得发生未批准变化；发现差异先固定反例并返回确认。以上均为待执行计划，不是实现或回归通过结果。
- 奖金：无公司历史算例残留、配置缺失明确提示、原普通计算回归。
- 邮件：测试与正式状态隔离、默认不发送、安装者自配、收件角色落地页权限正确。
- 不将运行代码复制到公开库后才清理敏感信息。

## 阶段四：云端测试和封装

- 使用GitHub托管的Windows runner；不启用自托管runner、付费扩容或长期开机云环境。
- CI仅安装锁定依赖并运行合成测试，限制工作流权限，不读取公司secrets。
- 覆盖模块回归、权限与占用、上传/解析、导出、备份恢复、语法、依赖/秘密/包清单扫描。
- 打包产物和哈希放GitHub Actions artifacts；不下载至开发机。不能用CI通过替代实际浏览器和干净机器安装验收。
- 通过受支持的云端浏览器/验收环境检查角色页面；若云端能力不可用，单列未验收项并请求选择，不擅自落地本机副本或降低标准。

### 局域网分层测试（计划，尚未执行）

1. 完成并获批网络规格后，先在隔离环境用虚构数据验证本机初始化、局域网开启、端口/防火墙范围与HTTPS访问；仅能打开登录页不算业务通过。
2. 在独立测试主机和第二台客户端，分别用虚构Admin与员工完成项目、付款、发票、奖金/月结的相关操作，核对主机保存结果和两端刷新一致；同时验证越权访问被拒绝。
3. 检查断网/恢复、连续点击、两个会话同时提交、服务重启和端口冲突：不重复编号或保存、不损坏数据，错误提示可见，重启后的会话处理符合规格。
4. 按OCR已安装/未安装两种情况检查文本PDF、扫描PDF和图片；未安装不伪装识别成功，也不绕过发票校验。邮件链接用模拟发送核对地址，不投递真实业务邮件。
5. 封装完成后在干净测试环境重新执行安装、初始化及上述双机流程，核对版本/产物哈希；不能用开发代码验证替代发行包验证。

GitHub托管runner提供云端构建与测试环境，不能证明使用者局域网、防火墙和证书已可用。[运行环境依据](https://docs.github.com/en/actions/concepts/runners/github-hosted-runners)。真实双机验收前需用户指定测试设备并授权安装；不得擅自在开发机保存公开包、使用公司正式主机或租用云资源。证据只保留脱敏结果至仓库或云端产物。

## 阶段五：验收与发布

审查变更及许可证、确认安装/升级/数据保留、检查源码/提交/文档/CI/发行包敏感内容。未通过不发布可用版本；正式Release与合并另经用户确认。原公司系统全程不变。

## 2026-09-10 服务费配置核心执行子计划（主控批准）

依据用户本轮“不内置，全部外显”的产品确认及既有条件上传授权，批准以下可独立验证的子步骤；奖金、页面及HTTP接入仍按后续分步计划，不冒充整份Plan通过。

- 修改位置仅公开候选分支。先更新本Spec/Plan，再创建新编合成测试；对只读来源模块执行预期失败测试，随后仅在内存制作公开版适配并上传。
- 允许文件：service-fee-config.js、tools/tests/public-service-fee-config.test.js、本批公开审查记录及治理文档。不修改来源文件；不上传任何公司参数、原算例、数据或私有历史。
- 保留费用单项枚举、函数API及既有费率范围；兼容导出SERVICE_FEE_DEFAULT_RATES但它是冻结的空对象。effectiveServiceFeeRates对缺配置返回空表供配置页面读取，对显式非法配置拒绝；不合并内置数值。
- validateServiceFeeRates按既有必填项和数值类型校验，返回新对象；serviceFeeRateForItemName先验证配置齐全，再使用明确的单项值或Admin填写的基础费率。错误提供可用于未来页面的明确原因；既有明确0仍有效。
- 固定反例：空配置不得生成费率；缺配置不得计算；null/空串/布尔/字符串数值/越界非有限值与未知项不得通过；原型继承的字段不能冒充已配置项；调用不得回写配置。
- 上传前完整阅读、定向敏感标记与语法检查、纯内存单元测试；上传后回读所有新增源码/测试并复跑两批测试、核对SHA256和文件允许清单。测试仅虚构参数，绝不作为安装默认值或种子。
- 本批不更改服务费乘法、代付判定、旧快照读取、奖金计算公式或锁定/月结规则；尚无公开版server/Admin页面，不能据本批声称API、UI、历史冻结已验收。
- 下一步的联动范围：去掉Admin页面的数值兜底；服务端保存/读取/未配置门禁；奖金可见参数及无公司历史回退；规则快照、权限、审计和真实浏览器验收。对缺历史快照的数据不得用新配置静默补算。
- 不写本机公开文件、不创建任务或工作树、不运行CI、不创建PR/合并/Release、不访问或改变内部系统。其余未决范围与许可证保持待确认。

## 2026-09-10 奖金核心关联子计划（主控批准）

用户同意在已确认“全部外显”的规则下继续。任务仍为L2、当前任务串行执行，无新Agent/工作树。只读既有代码，所有公开产物仅保存在本仓库。

1. 先更新上节奖金Spec及本Plan，允许新增bonus-config.js，适配bonus-preview.js、execution-bonus.js，新增tools/tests/public-bonus-config.test.js，以及本批记录和治理文档。不得原样上传公司默认表、历史算例或固定姓名。
2. 先在内存对原两个模块运行固定反例：无显式配置不得试算、动态人工阈值应生效、锁定快照应保存完整规则、过期规则不得锁定；公司参数仅在本机只读证据中核对，不公开失败输出中的原值。
3. 新配置核心严格验证表格/数值/范围/重叠；空配置只读展示为未配置。规范化规则生成SHA256摘要，作为预览→确认的内容一致性检查，不作为权限或秘密使用。
4. 奖金预览保持财务/付款选择/月筛选公式，删除公司回退；执行奖金保持金额、人员、付款和重复锁定门禁，接入显式规则/摘要并完整保存快照。只去掉已确认移除的公司特名依赖。
5. 以新编虚构参数验证正常公式、明确0、缺配置、边界/重叠/无界限、人工确认、旧预览与新配置冲突、快照深拷贝、付款版本/人员及月筛选；与来源可公开通用函数对照相同合成数据的金额结果。
6. 上传前逐文件全文审查、定向敏感标记、语法与单元测试；上传后回读源码/测试哈希并复跑基础、服务费及奖金全部单元测试，核对文件允许清单。纯内存测试，不产生本地公开文件或业务数据。
7. Admin字段矩阵随Spec固定。本轮不上传尚未全文脱敏的admin.html/server.js，不建替代管理页面冒充完整系统；下一步在对应来源公开审查完成后接入config.bonusRules、页面下拉/百分比、后台权限/保存原子性/摘要和历史展示，完成真实角色页面与跨模块回归。
8. 未运行HTTP、真实浏览器、云端CI或安装的项目保持未验收；不得因本批通过宣布全部外显、整站脱敏或可安装。无PR、合并、Release、内部数据/代码/服务或正式主机操作。

## 2026-09-10 持续执行预检：初始化与云端验收门禁

用户要求持续执行至完成，重大问题先沟通。本轮核对服务端启动、首次账号创建依赖、运行平台和公开版验收路径；未把持续执行解释为合并或发布授权。

已确认：使用原启动加载代码段及虚构内存文件完成4项检查。不存在文件返回空数据；非法JSON数据、非法JSON配置均仅记录错误后继续返回空状态；合法虚构数据仍保留。该检查没有启动完整服务、没有执行保存、没有读取真实数据或产生本地文件。

关键风险（基于上述证据的推测）：若公开版首次Admin创建仅按空用户列表判断，损坏的已有文件可能被误当成新安装，后续保存存在覆盖旧内容的风险。未发现或宣称任何真实实例已经丢失数据。此启动逻辑尚未上传公开候选。

建议提交用户确认的实现边界：
- 首版Windows，本机一次性创建Admin，无默认账号/密码；初始化后入口关闭，局域网不得抢先创建首个管理员。
- 真正首次缺少数据与已有文件损坏分开处理。已存在文件不可读、解析失败或关键结构非法时拒绝启动，保留原件，不自动重置；升级、重启不触发初始化。
- 公开版不携带服务器命令执行类运维入口；保留业务Admin、备份恢复与现有角色业务权限。原来源该类入口默认关闭，并非已证明匿名可执行。
- 完整HTTP、浏览器和封装测试需要云端隔离环境。建议批准仅本公开库GitHub托管Windows测试和候选包产物；只用虚构数据，不使用公司secrets、自托管runner、付费扩容或正式实例。本机不保留公开副本。
- 当前尚不创建CI工作流，不合并main、不创建PR或Release；许可证与实际局域网双机验收仍为发布前独立未决项。

当前可复核结果仍为6个核心模块、3份单测（146项）及200组合成差分，尚无可安装系统。方案与本次持续执行的完整技术Plan批准后，按服务端/页面→跨模块与安全→云端门禁→候选封装顺序继续，不在普通子步骤重复索要上传许可。

状态：初始化/平台与云端验收方案待用户确认；已通过核心成果保留。内部源文件、真实数据、附件、配置、服务及正式主机均未修改。公开记录仅保存在独立仓库。

## 2026-09-10 导出权限补修与许可证门禁（主控批准）

依据用户本次明确确认，继续原 L2 任务串行执行。允许内存适配 export-service.js、新建纯合成导出专项、云端真实格式验证，更新规格/验收记录；不修改公司来源或任何业务资产，不创建本地公开副本。

1. 先固定执行员工本人 25，却收到业务 150 / 执行合计 90 / 项目合计 240 的失败反例，再收紧普通员工可见记录。
2. 先裁剪后筛选；以允许字段重建本人奖金、月结记录，不复用完整原对象。Admin 表格和公式保持；来源数据深比较不变。供应商统计既定“涉及付款单数”表头同步到导出，不改变数量计算。
3. JSON/CSV/XLSX 与审计/元数据同源；测试角色、筛选、详情、隐藏快照和附件，修复后上传已审查文件，托管 Windows CI 全量复跑。
4. 核对依赖锁文件、许可证元数据及实际分发材料，建立打包许可核验；不开具法律保证、不猜作者或公司权利归属。具体 MIT/其他许可选择与署名待用户确认，未确认不生成虚假已授权的 LICENSE。
5. 补修不代表完整服务/浏览器/安装或发行通过。其余已批准公开化流程继续，PR、main 合并、Release 和正式部署仍不在本次授权内。

### 本轮许可证实现顺序

1. 先为缺失 LICENSE、元数据冲突、免责声明被截断及第三方说明缺失建立失败检查。
2. 写入标准 MIT 与中性项目贡献者署名；同步 package 与 lock 根 license 为 MIT，保留 private:true 防误发布 npm。
3. 核验脚本必须先校验项目许可，再收集第三方原文，云端许可材料包包含根 LICENSE、第三方说明和校验哈希；材料包不是完整安装包。
4. CI 用纯合成许可反例和现有 310 项回归复验；检查缺项不允许通过。记录准确提交、测试和未完成门禁。
